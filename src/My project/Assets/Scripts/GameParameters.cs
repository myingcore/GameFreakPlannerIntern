using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameParameters : MonoBehaviour
{
    // parameter about time (second)
    public float t_cooldownCatch = 3;
    public float t_cooldownThrow = 3;

    // parameter about speed
    public float s_normal_player = 0.05f;
    public float s_normal_ball = 500f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
